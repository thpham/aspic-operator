apiVersion: v1
data:
  ui.bannercontent: "This is a demo installation for the aspic-operator. Not for production use!"
  ui.bannerpermanent: "true"
  exec.enabled: "true"
kind: ConfigMap
metadata:
  labels:
    app.kubernetes.io/name: argocd-cm
    app.kubernetes.io/part-of: argocd
  name: argocd-cm
